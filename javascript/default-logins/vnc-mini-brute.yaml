id: vnc-mini-brute

info:
  name: VNC Mini Brute - 2 Password Dictionary
  author: penetration-team
  severity: high
  description: |
    Tests two approved VNC passwords with one thread and stops at the first match.
    MAX 2 ATTEMPTS. Intended only for explicitly authorized credential checks.
  metadata:
    max-request: 2
  tags: js,vnc,default-login,network

javascript:
  - pre-condition: |
      isPortOpen(Host,Port);
    code: |
      var vnc = require('nuclei/vnc');
      var client = new vnc.VNCClient();
      client.Connect(Host, Port, Password);
    args:
      Host: "{{Host}}"
      Port: "5900"
      Password: "{{passwords}}"
    threads: 1
    attack: pitchfork
    stop-at-first-match: true
    payloads:
      passwords:
        - admin
        - password
    matchers:
      - type: dsl
        dsl:
          - "success == true"
          - "response == true"
        condition: and
# digest: 4b0a00483046022100f4b030316a6cca992629c160ed6cd3ff8416b064544151e914f85812488f0252022100f7468a41a143d05f8cdc5563ac077ab22dedb188dd0d46e19146fa8d80db955a:7ec931ac1b7d2e9f67f639e8d62a6615