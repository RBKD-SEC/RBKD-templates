id: kubernetes

info:
  name: Kubernetes Scan
  author: internal
  description: |
    Service-specific scan for kubernetes. Covers: security exposure checks.
  tags: workflow,kubernetes

workflows:
  - template: http/technologies/kubernetes/kube-api/kube-api-version.yaml
    subtemplates:
      - template: RBKD-templates/http/exposures/kubernetes-api-unauth.yaml
