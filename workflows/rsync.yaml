id: rsync

info:
  name: Rsync Scan
  author: internal
  description: |
    Service-specific scan for rsync. Covers: fingerprint detection, security exposure checks.
  tags: workflow,rsync

workflows:
  - template: RBKD-templates/network/detection/rsync-detect.yaml
    subtemplates:
      - template: RBKD-templates/network/misconfig/rsync-anonymous-list.yaml
