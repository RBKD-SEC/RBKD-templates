id: weblogic

info:
  name: Weblogic Scan
  author: internal
  description: |
    Service-specific scan for weblogic. Covers: fingerprint detection, security exposure checks.
  tags: workflow,weblogic

workflows:
  - template: http/technologies/weblogic-detect.yaml
    subtemplates:
      - template: http/exposed-panels/weblogic-login.yaml
      - template: http/default-logins/weblogic/weblogic-weak-login.yaml
