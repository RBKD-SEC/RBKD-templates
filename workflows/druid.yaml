id: druid

info:
  name: Druid Scan
  author: internal
  description: |
    Service-specific scan for druid. Covers: security exposure checks.
  tags: workflow,druid

workflows:
  - template: http/exposed-panels/druid-console-exposure.yaml
    subtemplates:
      - template: http/misconfiguration/druid-monitor.yaml
