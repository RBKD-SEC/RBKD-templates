id: activemq

info:
  name: Activemq Scan
  author: internal
  description: |
    Service-specific scan for activemq. Covers: authentication checks.
  tags: workflow,activemq

workflows:
  - template: RBKD-templates/http/default-logins/activemq-mini-brute.yaml
