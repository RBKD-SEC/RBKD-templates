id: activemq

info:
  name: Activemq Scan
  author: internal
  severity: info
  description: |
    Service-specific scan for activemq. Covers: authentication checks.
  tags: workflow,activemq

workflows:
  - template: http/exposed-panels/activemq-panel.yaml
    subtemplates:
      - template: http/default-logins/activemq/activemq-default-login.yaml
      - template: http/default-logins/activemq/activemq-artemis-default-login.yaml
