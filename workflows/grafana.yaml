id: grafana

info:
  name: Grafana Scan
  author: internal
  description: |
    Service-specific scan for grafana. Covers: fingerprint detection, authentication checks.
  tags: workflow,grafana

workflows:
  - template: http/exposed-panels/grafana-detect.yaml
    subtemplates:
      - template: http/default-logins/grafana/grafana-default-login.yaml
      - template: http/misconfiguration/grafana-unauth-access.yaml
      - template: http/misconfiguration/grafana-public-signup.yaml
