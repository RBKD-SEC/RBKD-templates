id: grafana

info:
  name: Grafana Scan
  author: internal
  description: |
    Service-specific scan for grafana. Covers: fingerprint detection, authentication checks.
  tags: workflow,grafana

workflows:
  - template: http/exposed-panels/grafana-detect.yaml
  - template: RBKD-templates/http/default-logins/grafana-mini-brute.yaml
