id: postgres

info:
  name: Postgres Scan
  author: internal
  description: |
    Service-specific scan for postgres. Covers: fingerprint detection, authentication checks.
  tags: workflow,postgres

workflows:
  - template: RBKD-templates/network/detection/postgres-detect.yaml
  - template: RBKD-templates/network/default-login/postgres-mini-brute.yaml
