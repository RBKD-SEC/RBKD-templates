id: omron

info:
  name: Omron Scan
  author: internal
  description: |
    Service-specific scan for omron. Covers: fingerprint detection, authentication checks.
  tags: workflow,omron

workflows:
  - template: RBKD-templates/http/technologies/omron-detect.yaml
