id: gitlab

info:
  name: Gitlab Scan
  author: internal
  description: |
    Service-specific scan for gitlab. Covers: fingerprint detection.
  tags: workflow,gitlab

workflows:
  - template: RBKD-templates/http/technologies/gitlab-detect.yaml
