id: rocketmq

info:
  name: Rocketmq Scan
  author: internal
  description: |
    Service-specific scan for rocketmq. Covers: fingerprint detection, security exposure checks.
  tags: workflow,rocketmq

workflows:
  - template: http/exposed-panels/rocketmq-console-exposure.yaml
    subtemplates:
      - template: network/misconfig/apache-rocketmq-broker-unauth.yaml
