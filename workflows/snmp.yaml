id: snmp

info:
  name: Snmp Scan
  author: internal
  description: |
    Service-specific scan for snmp. Covers: fingerprint detection, security exposure checks.
  tags: workflow,snmp

workflows:
  - template: RBKD-templates/network/detection/snmp-detect.yaml
  - template: RBKD-templates/network/misconfig/snmp-public-community.yaml
