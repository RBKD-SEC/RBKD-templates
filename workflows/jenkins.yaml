id: jenkins

info:
  name: Jenkins Scan
  author: internal
  description: |
    Service-specific scan for jenkins. Covers: fingerprint detection, security exposure checks, authentication checks.
  tags: workflow,jenkins

workflows:
  - template: http/technologies/jenkins-detect.yaml
    subtemplates:
      - template: http/exposed-panels/jenkins-login.yaml
      - template: http/default-logins/jenkins/jenkins-default.yaml
      - template: http/misconfiguration/jenkins/jenkins-users-exposure.yaml
      - template: http/vulnerabilities/jenkins/jenkins-script.yaml
