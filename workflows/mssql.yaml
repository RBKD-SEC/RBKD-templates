id: mssql

info:
  name: Mssql Scan
  author: internal
  description: |
    Service-specific scan for mssql. Covers: fingerprint detection, authentication checks.
  tags: workflow,mssql

workflows:
  - template: javascript/detection/mssql-detect.yaml
  - template: RBKD-templates/network/default-login/mssql-mini-brute.yaml
