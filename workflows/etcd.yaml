id: etcd

info:
  name: Etcd Scan
  author: internal
  description: |
    Service-specific scan for etcd. Covers: security exposure checks.
  tags: workflow,etcd

workflows:
  - template: http/technologies/kubernetes/etcd/etcd-version.yaml
    subtemplates:
      - template: RBKD-templates/http/exposures/etcd-unauth.yaml
