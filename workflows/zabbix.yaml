id: zabbix

info:
  name: Zabbix Scan
  author: internal
  description: |
    Service-specific scan for zabbix. Covers: fingerprint detection, authentication checks.
  tags: workflow,zabbix

workflows:
  - template: http/exposed-panels/zabbix-server-login.yaml
    subtemplates:
      - template: http/default-logins/zabbix/zabbix-default-login.yaml
      - template: http/misconfiguration/zabbix-dashboards-access.yaml
