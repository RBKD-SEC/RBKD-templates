id: zabbix

info:
  name: Zabbix Scan
  author: internal
  description: |
    Service-specific scan for zabbix. Covers: fingerprint detection, authentication checks.
  tags: workflow,zabbix

workflows:
  - template: RBKD-templates/http/technologies/zabbix-detect.yaml
