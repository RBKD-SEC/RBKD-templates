id: tomcat

info:
  name: Tomcat Scan
  author: internal
  description: |
    Service-specific scan for tomcat. Covers: fingerprint detection, security exposure checks, authentication checks.
  tags: workflow,tomcat

workflows:
  - template: http/technologies/apache/tomcat-detect.yaml
    subtemplates:
      - template: http/default-logins/apache/tomcat-default-login.yaml
      - template: http/misconfiguration/tomcat-directory-listing.yaml
      - template: http/exposed-panels/apache/public-tomcat-manager.yaml
