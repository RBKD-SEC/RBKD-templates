id: tomcat

info:
  name: Tomcat Scan
  author: internal
  description: |
    Service-specific scan for tomcat. Covers: fingerprint detection, security exposure checks, authentication checks.
  tags: workflow,tomcat

workflows:
  - template: RBKD-templates/http/technologies/tomcat-detect.yaml
  - template: RBKD-templates/http/exposures/git-config-exposed.yaml
  - template: RBKD-templates/http/default-logins/tomcat-manager-mini-brute.yaml
