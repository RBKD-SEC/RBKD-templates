id: kafka

info:
  name: Kafka Scan
  author: internal
  description: |
    Service-specific scan for kafka. Covers: fingerprint detection, security exposure checks.
  tags: workflow,kafka

workflows:
  - template: RBKD-templates/http/technologies/kafka-detect.yaml
  - template: RBKD-templates/http/exposures/kafka-connect-exposed.yaml
  - template: RBKD-templates/network/misconfig/kafka-unauth.yaml
