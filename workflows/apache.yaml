id: apache

info:
  name: Apache Scan
  author: internal
  description: |
    Service-specific scan for apache. Covers: fingerprint detection, security exposure checks.
  tags: workflow,apache

workflows:
  - template: http/technologies/apache/apache-detect.yaml
  - template: RBKD-templates/http/exposures/apache-server-status-exposed.yaml
  - template: http/miscellaneous/directory-listing.yaml
