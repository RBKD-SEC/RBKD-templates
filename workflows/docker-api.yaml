id: docker-api

info:
  name: Docker Api Scan
  author: internal
  description: |
    Service-specific scan for docker-api. Covers: security exposure checks.
  tags: workflow,docker-api

workflows:
  - template: http/misconfiguration/exposed-docker-api.yaml
    subtemplates:
      - template: http/misconfiguration/docker-daemon-exposed.yaml
