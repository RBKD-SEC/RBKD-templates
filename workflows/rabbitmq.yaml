id: rabbitmq

info:
  name: Rabbitmq Scan
  author: internal
  description: |
    Service-specific scan for rabbitmq. Covers: authentication checks.
  tags: workflow,rabbitmq

workflows:
  - template: RBKD-templates/http/default-logins/rabbitmq-mini-brute.yaml
