id: rabbitmq

info:
  name: Rabbitmq Scan
  author: internal
  severity: info
  description: |
    Service-specific scan for rabbitmq. Covers: authentication checks.
  tags: workflow,rabbitmq

workflows:
  - template: http/exposed-panels/rabbitmq-dashboard.yaml
    subtemplates:
      - template: http/default-logins/rabbitmq/rabbitmq-default-login.yaml
      - template: http/misconfiguration/rabbitmq-exporter-metrics.yaml
