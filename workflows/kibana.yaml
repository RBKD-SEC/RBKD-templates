id: kibana

info:
  name: Kibana Scan
  author: internal
  description: |
    Service-specific scan for kibana. Covers: fingerprint detection.
  tags: workflow,kibana

workflows:
  - template: http/exposed-panels/kibana-panel.yaml
    subtemplates:
      - template: http/misconfiguration/exposed-kibana.yaml
      - template: http/exposures/files/elastic-kibana-config.yaml
