id: memcached

info:
  name: Memcached Scan
  author: internal
  description: |
    Service-specific scan for memcached. Covers: fingerprint detection, security exposure checks.
  tags: workflow,memcached

workflows:
  - template: RBKD-templates/network/detection/memcached-detect.yaml
    subtemplates:
      - template: network/misconfig/memcached-stats.yaml
