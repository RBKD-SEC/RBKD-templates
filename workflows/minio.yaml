id: minio

info:
  name: Minio Scan
  author: internal
  description: |
    Service-specific scan for minio. Covers: fingerprint detection, authentication checks.
  tags: workflow,minio

workflows:
  - template: http/exposed-panels/minio-browser.yaml
    subtemplates:
      - template: http/default-logins/minio/minio-default-login.yaml
