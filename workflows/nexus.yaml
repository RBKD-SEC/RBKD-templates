id: nexus

info:
  name: Nexus Scan
  author: internal
  description: |
    Service-specific scan for nexus. Covers: fingerprint detection, authentication checks.
  tags: workflow,nexus

workflows:
  - template: http/technologies/nexus-detect.yaml
    subtemplates:
      - template: http/default-logins/nexus/nexus-default-login.yaml
      - template: http/misconfiguration/nexus-repository-anonymous-access.yaml
